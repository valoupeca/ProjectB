@model IEnumerable<ProjectBv2.Models.Postes>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.adresse)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.kilometrage)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.adresse)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.kilometrage)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_postes }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_postes }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_postes })
        </td>
    </tr>
}

</table>
